import {message} from 'ant-design-vue'
import interceptorSrc from './interceptorSrc'
// transition...
export default {
    // install(app) {
    //     app.config.globalProperties.$message = message
    // }
}

export {
    interceptorSrc
}